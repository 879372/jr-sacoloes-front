[build]
builder = "nixpacks"

[build.env]
NIXPACKS_NODE_VERSION = "22"

[deploy]
startCommand = "npm run preview -- --port $PORT --host"
numReplicas = 1

[[deploy.regions]]
region = "us-east"
cpuLimit = 1000
memoryLimit = 1024
